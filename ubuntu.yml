- hosts: localhost
  become: true
  pre_tasks:
  - name: Update Apt
    become: true
    apt:
      force_apt_get: true
      update_cache: true
      state: present
    tags:
      - install
      - core
  # vars: ...
  tasks:
  - include: tasks/zsh.yml
  - include: tasks/asdf.yml
